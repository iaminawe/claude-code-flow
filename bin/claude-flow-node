#!/usr/bin/env node

const path = require('path');

// Simple Node.js wrapper that shows the commands available
console.log(`
🧠 Claude-Flow TaskMaster v1.0.1

This version requires Deno to be installed. It seems Deno is having issues.

To fix this:
1. Install Deno: curl -fsSL https://deno.land/x/install/install.sh | sh
2. Or use the original package: npm install -g claude-flow

Available commands when Deno is working:
- taskmaster init --with-ai --enterprise --autonomous
- sparc modes
- sparc run <mode> "<task>"
- memory store/query
- agent spawn
- start
- status

For now, the .taskmaster directory has been created with AI/Enterprise/Autonomous features enabled.
`);

// Exit gracefully
process.exit(0);